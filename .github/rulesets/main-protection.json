{
  "name": "Temple main protection — recommended",
  "target": "branch",
  "enforcement": "active",
  "conditions": { "ref_name": { "include": ["refs/heads/main"], "exclude": [] } },
  "rules": [
    { "type": "deletion" },
    { "type": "non_fast_forward" },
    { "type": "required_status_checks", "parameters": {
      "strict_required_status_checks_policy": true,
      "required_status_checks": [
        { "context": "validate" },
        { "context": "validate-and-publish" }
      ]
    } }
  ],
  "notes": "Repository policy template. Apply through GitHub Rulesets/branch protection when the connected API exposes repository-settings writes. Preserve an owner emergency bypass if desired."
}
