name: Apply v5.2.4 selection fix

on:
  push:
    branches: [feature/v5.2.4-living-codex]
    paths:
      - '.github/workflows/apply-v524-selection-fix.yml'
      - 'scripts/patch-v524-selection.py'

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - name: Patch explicit Codex selection
        run: python scripts/patch-v524-selection.py
      - name: Validate patched release
        run: |
          node scripts/validate-v5.mjs --release
          node scripts/validate-v5.2.4.mjs
          node --check scripts/v5.2.4-living-codex.js
      - name: Commit fix
        run: |
          git config user.name "Temple Release Bot"
          git config user.email "actions@users.noreply.github.com"
          git add scripts/v5.2.4-living-codex.js
          git diff --cached --quiet && exit 0
          git commit -m "fix: honor explicit Living Codex record selection"
          git push origin HEAD:feature/v5.2.4-living-codex
